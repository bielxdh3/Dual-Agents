{
  "schema_version": 1,
  "request_id": "aip-phase8-voice-core-correction-20260820",
  "parent_request_id": "aip-phase8-functional-voice-20260820",
  "action": "correct",
  "repository": "E:\\AIP",
  "task": "Continue the existing partial Phase 8 voice runtime implementation in E:\\AIP on branch feat/phase-8-functional-voice. Do not reset, discard, or broaden the work. Apply only the compile/test corrections below in the already changed voice.rs, database.rs, Cargo.toml, and migration 0022. Keep the implementation local-only, bounded, fail-closed, no raw audio persistence, no cloud/network, and preserve explicit fixture paths. Add focused unit tests for the corrected degraded/cancellation/limit paths if they are missing. Run cargo fmt --check, cargo check --locked, cargo clippy --locked --all-targets -- -D warnings, cargo test --locked --lib, and git diff --check. If all pass, commit the current Phase 8 core scope with exactly `feat: implement phase 8 functional local voice`. Do not push or perform GitHub operations. Do not add UI/contracts/commands in this correction; that will be a separate bounded integration pass.",
  "constraints": [
    "Use only the configured Biel4 App Server Executor; no native subagents.",
    "Preserve all current uncommitted Phase 8 changes; no reset, checkout discard, rebase, or force operation.",
    "No raw audio persistence, upload, telemetry, public listener, cloud provider, arbitrary shell, or safety weakening.",
    "Do not push, create PR, merge, tag, release, or modify unrelated phase scope.",
    "Return the wrapper-required JSON report fields only."
  ],
  "context_files": [
    "apps/desktop/src-tauri/src/voice.rs",
    "apps/desktop/src-tauri/src/database.rs",
    "apps/desktop/src-tauri/migrations/0022_phase8_voice_runtime.sql",
    "apps/desktop/src-tauri/Cargo.toml",
    "docs/PHASE_8_VOICE_SPEC.md"
  ],
  "review_findings": [
    {
      "title": "Blocking compile errors in the partial voice runtime",
      "severity": "blocking",
      "details": "cargo check --locked currently fails in apps/desktop/src-tauri/src/voice.rs: the closure results at approximately lines 372 and 737 need an explicit Result error type so DatabaseError::code() is inferred; the synthesis completion passes i64 duration_ms where finish_voice_operation requires u64. Fix these without changing behavior."
    },
    {
      "title": "Warnings fail the required clippy gate",
      "severity": "important",
      "details": "cargo check reports an unused timed_out assignment and unnecessary mut on two WAVEHDR values. Resolve these in the smallest safe way and preserve cleanup/cancellation semantics."
    },
    {
      "title": "Required focused core validation is absent",
      "severity": "important",
      "details": "The timed-out executor ran no validation. Add or preserve deterministic tests proving capture duration limits and explicit unavailable-provider/device degradation without synthetic hardware success, then run the required Rust gates."
    }
  ],
  "authorization": {
    "allowed_actions": ["local_mutation", "local_commit"]
  },
  "max_correction_cycles": 0
}
